{
  "name": "semantic-flow-scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsx src/build-skill.ts",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npm run build && node --test --test-concurrency=1 tests/cli-contract.test.mjs tests/semantic-review/artifact-commands.test.mjs tests/semantic-review/history-commands.test.mjs tests/semantic-review/publication-commands.test.mjs tests/review-feedback/draft-commands.test.mjs tests/review-feedback/resolution-commands.test.mjs"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "jsonc-parser": "3.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
